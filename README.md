# Wikipedia-RandomURL-Generator
A simple Python Script that allows the user to grab as many random URL's from Wikipedia. Can be used for other websites as well that contains a random redirection URL.
